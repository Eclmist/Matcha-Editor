<UserControl x:Class="Matcha_Editor.MVVM.View.DockingTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Matcha_Editor.MVVM.View"
             mc:Ignorable="d" 
             MouseDown="UserControl_MouseDown" 
             >
    <Border Style="{StaticResource PanelTabStyle}" Width="Auto" Padding="8,2,8,2">
        <Grid Width="Auto">
            <StackPanel Orientation="Horizontal">
                <Border CornerRadius="5" Width="12" Height="12" Background="{StaticResource Editor.Window.PrimaryBrush}">
                </Border>
                <Label x:Name="Title" Content="{Binding TitleText}" Foreground="{StaticResource Editor.Window.SystemFontBrush}" 
                       FontSize="12" Padding="0,0,0,0" Margin="4,0,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
