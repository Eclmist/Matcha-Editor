<Window x:Class="Matcha_Editor.MVVM.View.DockingPreviewWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:custom="clr-namespace:Matcha_Editor.MVVM.View"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Title="DockingPreviewWindowView" Height="150" Width="200">
    <DockPanel Margin="1,2,1,1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <custom:DockingTabView x:Name="Tab" Opacity="0.9" DockPanel.Dock="Top" Panel.ZIndex="2" Grid.Row="0" HorizontalAlignment="Left" Margin="0"/>
        <Border 
            Opacity="0.9" 
            DockPanel.Dock="Bottom" 
            Panel.ZIndex="1" 
            Margin="0,-2,0,0" Style="{StaticResource PanelContentStyle}">
        </Border>
    </DockPanel>
</Window>
