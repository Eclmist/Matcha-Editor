<UserControl x:Class="Matcha_Editor.MVVM.View.ViewportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:custom="clr-namespace:Matcha_Editor.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Border Style="{StaticResource PanelTabStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="23"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid HorizontalAlignment="Stretch">
                <Label Content="World Viewer" Foreground="#CCCCCC" FontSize="12" Padding="0,3,0,0" Margin="6,0,0,0"/>
            </Grid>

            <DockPanel Grid.Row="1">
                <!-- The hosted window exists in the border -->
                <custom:RenderSurfaceView/> 
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
