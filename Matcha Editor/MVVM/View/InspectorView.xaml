<UserControl x:Class="Matcha_Editor.MVVM.View.InspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <DockPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Border DockPanel.Dock="Bottom" Panel.ZIndex="1" BorderThickness="0" Margin="0,-1,0,0" Style="{StaticResource PanelContentStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <!--<RowDefinition Height="23"/>-->
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <!--<Grid HorizontalAlignment="Stretch">
                <Label Content="Inspector" Foreground="#CCCCCC" FontSize="12" Padding="0,3,0,0" Margin="6,0,0,0"/>
            </Grid>-->

                <StackPanel Background="#252526" Grid.Row="1">
                </StackPanel>
            </Grid>
        </Border>
    </DockPanel>
</UserControl>
